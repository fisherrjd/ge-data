# Copy to .env and set values. .env is gitignored.
# Postgres role password (docker-compose.yml).
POSTGRES_PASSWORD=change-me-to-something-strong
# Wiki API requires a descriptive User-Agent (project + a contact) for the polls,
# or it may block you.
USER_AGENT=ge-data (contact: you@example.com)
