# Copy to .env and set values. .env is gitignored.
# Postgres role password (docker-compose.yml).
POSTGRES_PASSWORD=change-me-to-something-strong
# Wiki API requires a descriptive User-Agent identifying the project + a contact,
# or it may block you.
INGESTER_USER_AGENT=ge-data ingester (contact: you@example.com)
